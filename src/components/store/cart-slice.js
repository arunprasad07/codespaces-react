const { createSlice } = require("@reduxjs/toolkit");

createSlice({
    name: 'cart',
    initialState:{},
    reducers: {
        addItemToCart(){

        },
        removeItemFromCart(){
            
        }
    }
})